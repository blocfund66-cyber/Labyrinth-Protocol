# Render Infrastructure as Code (Blueprint) for Labyrinth Protocol
services:
  - type: web
    name: labyrinth-protocol
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
